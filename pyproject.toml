[tool.ruff]
target-version = "py310"
line-length = 100

[tool.ruff.lint]
select = ["E", "F", "I", "W"]

[tool.ruff.lint.per-file-ignores]
"*.ipynb" = ["E501", "W293"]

[tool.pytest.ini_options]
asyncio_mode = "auto"
