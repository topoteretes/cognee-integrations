name: Lint

on:
  pull_request:
    branches: [main]
    paths:
      - 'integrations/**/*.py'
      - 'scripts/**/*.py'
      - 'pyproject.toml'
  push:
    branches: [main]
    paths:
      - 'integrations/**/*.py'
      - 'scripts/**/*.py'
      - 'pyproject.toml'

jobs:
  ruff:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.10"

      - name: Install uv
        uses: astral-sh/setup-uv@v4

      - name: Install ruff
        run: uv tool install ruff

      - name: Check formatting
        run: ruff format --check integrations/

      - name: Run linter
        run: ruff check integrations/
