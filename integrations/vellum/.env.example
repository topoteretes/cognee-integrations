# Cognee Cloud, or a self-hosted Cognee server (remember/recall talk to this):
COGNEE_BASE_URL=
COGNEE_API_KEY=

# For local Cognee (no server) — the LLM key Cognee uses to build the graph:
LLM_API_KEY=
LLM_MODEL=gpt-4o-mini
